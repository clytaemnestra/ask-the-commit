# RUNTIME dependencies — everything the deployed query service needs, and
# nothing else. Deliberately free of torch, sentence-transformers, chromadb and
# faster-whisper: those are ingest-only and would not fit a free tier
# (torch alone is ~800MB resident).
#
# The question is embedded locally by ONNX Runtime (~90MB resident, a 23MB
# committed model) and the index is a committed NumPy array, so answering a
# question needs no ML framework and no embedding API key.
#
# For transcription and index building, use requirements-ingest.txt.

fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0   # typed config; pure Python, pulls only python-dotenv
numpy>=1.26,<3.0              # the vector index and its dot-product search
onnxruntime>=1.17,<2.0        # runs the committed MiniLM export locally, without torch
tokenizers>=0.15,<1.0         # WordPiece tokenisation for that model
httpx>=0.27,<1.0              # optional hosted embedding providers
openai>=1.40,<3.0             # OpenAI-compatible chat client (Groq / Ollama / OpenAI)
